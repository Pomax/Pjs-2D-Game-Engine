/**
 * We must set the screen dimensions to something.
 */
final int screenWidth = 200;
final int screenHeight = 200;

/**
 * Right now, the initialize function doesn't
 * actually contain any code, so nothing much
 * will seem to be happening.
 */
void initialize() {}
